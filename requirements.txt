# --- Research Vault v2 runtime ----------------------------------------------
# Keep this product dependency in sync with runtime/requirements.txt. Source
# converters remain optional external adapters and are not installed here.
PyYAML==6.0.3

# --- Dev / test -------------------------------------------------------------
# Run the test suite with: pip install -r requirements-dev.txt
