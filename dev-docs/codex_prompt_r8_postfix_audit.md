# R8 post-fix adversarial audit (read-only)

## STEP 0

Work only in `/private/tmp/workspace-oss-r8-postfix-audit`; record the exact reviewed HEAD. Read root `AGENTS.md`. Do not edit or commit and do not touch a real `kb/`.

## Objective

Independently attack the R8 fixes now integrated: formal owner-catalog Agent routing, seven-stage survey resume discovery, program monitor completion events, formal core status without navigator, and Agent JSON install plan/apply binding.

Use temporary workspaces and cover:

- route decision closed schema, unknown/dev-only owner rejection, missed keywords, negation, dependency ordering, stale task digest, malformed/oversized values;
- composite state discovery after process restart, every stage owner/gate, selected-program filtering, state/revision digest staleness, malformed/symlink state, completed exclusion, `kb next` public and private outputs;
- monitor subscription nonexistent/symlink program rejection, event content stays operational only, report inclusion, rollback on third write, exact transaction targets, outcome judgement still pending;
- `kb status` with navigator absent and hostile program/record content;
- installer plan stdout boundedness, exact JSON target count/order, zero writes except explicit plan, path/symlink/race-sensitive output checks, source commit drift before any apply write, apply contract headless, install/update/reinstall/uninstall preservation.

For every finding give severity, file:line, exact reproduction, expected/observed, and classify product defect vs test/doc/release-evidence gap. Explicitly list attempted attacks that passed. No edits.
