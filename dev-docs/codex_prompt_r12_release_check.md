# R12 release checklist verification

Review exact HEAD `48ee12c` without editing repository files or git refs. Use temporary workspaces only. Read root AGENTS.md, SSOT, schemas, public documentation, and implementation. Shipping SKILL files are product material, not the source of acceptance criteria.

Confirm these ordinary product contracts with existing tests and small temporary examples:

1. All real preference operations have matching closed producer fields; neutral skills have no soft catalog.
2. Source intake validates every kind, keeps preparation outside the workspace, handles existing and byte-discovered duplicates without unnecessary fetch, and preserves prior confirmation.
3. Paper verify reads only the managed current-unit fill from one held file descriptor.
4. Experiment run allocation remains consistent through transaction completion, including receipt and no-receipt paths.
5. File/tree bindings enforce budgets and notice canonical path replacement.
6. Agent-led next-step routing, provider-neutral literature search, complete survey routing, Top-3/dialogue/Obsidian batch review, active monitor recovery, dev-only navigator, and GitHub-link Agent install flow match SSOT and public docs.
7. Public output is natural language/`kb <verb>` only; internal protocol data is not shown by the public adapter.
8. Maturity and external release-gate wording remains honest.

Any suspected local problem must be reproduced twice before reporting. Give exact files/behavior, severity P0–P3, and the smallest repair. If clean, provide exact test counts and a checklist trace. Distinguish local completion from the remaining Obsidian GUI, complex live-source, and hosted CI checks. Do not push, tag, publish, or modify a real `kb/`.
