# R3 Track C — survey freshness, experiment run identity, OpenAlex scout

## STEP 0 — base sync

Work only in the assigned worktree. Verify HEAD is exactly `d633480115a75c6ca55a4430b73a7e5985c218c9`; verify current `literature-synthesizer/scripts/synthesize.py`, `experiment-workbench/scripts/experiment.py`, and `research.sources.stage_search_results` exist. The current checkout intentionally has no `literature-scout`; it is a new R3 skill. Wrong base means STOP and report.

Read main `AGENTS.md`, R3 SSOT sections 3.4A/3.6/3.9, and the system skill-creator instructions at `/Users/czx/.codex/skills/.system/skill-creator/SKILL.md`. Shipping skills are product source, not design rules.

## Ownership — only edit/create these files

- `.agents/skills/literature-synthesizer/scripts/synthesize.py`
- `.agents/skills/literature-synthesizer/SKILL.md`
- `.agents/skills/experiment-workbench/scripts/experiment.py`
- `.agents/skills/experiment-workbench/SKILL.md`
- `.agents/lib/research/sources.py` only to preserve a narrow candidate provenance mapping
- `.agents/lib/research/openalex.py` (new narrow client)
- `.agents/skills/literature-scout/**` (new skill)
- `.agents/lib/research/tests/test_literature_synthesizer.py`
- `.agents/lib/research/tests/test_experiment_validator.py`
- `.agents/lib/research/tests/test_literature_scout.py` (new)

Do not edit SCHEMAS, DESIGN/README/version, source-intake implementation/docs, orchestrator, kb-cli, shared recovery modules, or real `kb/`. Root integrates shared docs/count/version.

## A. Survey consumer binding

1. Prepare anchors each selected unit with canonical id/kind/title, normalized record content digest, confirmation receipt digest, and digests for cited/readable evidence artifacts. Use deterministic shared/local helpers; scripts do not interpret content.
2. Verify reloads every current unit and compares exact anchor before writing verified survey. Missing/changed identity/content/confirmation/evidence fails closed with actionable internal errors.
3. Verified payload stores `consumer_binding` with selection filters, exact unit bindings, and verified time. Add a pure read `survey_staleness` helper in this owned module: detect changed/deleted units and newly matching units for the stored selection. It must not mutate files.
4. Preserve evidence-first fill/verify and pending judgement behavior; no auto-confirm, no script-authored trend/gap.

## B. Experiment run fingerprint

1. Add canonical fingerprint over experiment id, normalized tested hypothesis, sorted normalized changes, metric schema only (name/unit/direction), contained artifact identities, explicit `--config-revision`, and optional `--seed`. Result summary/time/observed metric values are excluded.
2. Run numbering, fingerprint duplicate check, Markdown and run-log writes must execute after lock acquisition. Fix the current lock-outside next path/record load pattern within this file without broad shared changes.
3. Same fingerprint + same seed/config revision fails unless explicit rerun mode with nonempty rerun reason. Different seed groups as repeat. Persist fingerprint, repeat index/group IDs, seed/config revision/rerun reason in run-log and Markdown.
4. Preserve typed metrics, anchors, artifacts, diagnosis evidence and exact transaction/checkpoint paths. No statistical judgement generated by script.

## C. OpenAlex literature-scout

1. Create the new skill with the system `init_skill.py` scaffold (existing project location is explicit), then remove placeholders and keep only SKILL.md, `agents/openai.yaml`, and necessary scripts. Keep SKILL concise and imperative; validate metadata.
2. Implement a stdlib client for one bounded Works search page. API key comes from `OPENALEX_API_KEY` at runtime or an injected test transport; never persist, print, log, include in exception strings, or place in URL-visible public output. Missing key, 429, malformed response and network error fail before staging.
3. Default 25, max 100, `select` only allowed fields; no cursor crawl. Map/dedupe stable OpenAlex/DOI candidates, exclude retracted by default but preserve an explicit include-retracted private option if implemented.
4. Reuse `stage_search_results` and source-search schema. Extend its candidate mapping narrowly to retain structured `provenance.openalex` facts without raw response/key. Reruns preserve manual note/status and avoid duplicate candidates.
5. No public kb verb. Skill user-facing behavior is natural language; no naked Python/flags/env/path in user-visible text. It stages candidates, never creates canonical papers or relevance claims.

## Tests / red lines / commits

- Network tests use fake transport/fixtures; no live key, no real external writes.
- Add negative tests for key redaction, 429/malformed no-write, duplicate idempotence, survey stale cases, fingerprint collision/rerun/seed/concurrency.
- All writes transaction/lock/atomic/CAS; analyzer scripts never author understanding.
- Run focused tests, skill quick validation/metadata, `git diff --check`.
- Small commits per subsystem, no push. Final report commits/tests and any interface root must integrate.
