interface:
  display_name: "Repo Cataloger"
  short_description: "Ingest and normalize reusable repositories into the shared repo library"
  default_prompt: "Use $repo-cataloger to import a local repo or GitHub URL into doc/research/library/repos/, detect duplicates, and generate reusable repo metadata."

