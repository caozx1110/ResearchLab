interface:
  display_name: "Weekly Report Author"
  short_description: "Time-bounded program reporting from reporting events and durable artifacts"
  default_prompt: "Use $weekly-report-author to query reporting events plus durable program artifacts, then write auditable weekly wiki reports that capture high-value trends, blockers, and progress deltas without inventing missing discussion or experiment history."
