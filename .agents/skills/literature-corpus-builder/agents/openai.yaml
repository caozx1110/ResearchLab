interface:
  display_name: "Literature Corpus Builder"
  short_description: "Ingest literature into the canonical library"
  default_prompt: "Use $literature-corpus-builder to import PDFs from raw/, canonicalize literature URLs, detect duplicates, and stage canonical literature entries before handing off note writing to $research-note-author."
