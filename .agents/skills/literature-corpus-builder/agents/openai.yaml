interface:
  display_name: "Literature Corpus Builder"
  short_description: "LLM-wiki ingest owner for canonical literature entries"
  default_prompt: "Use $literature-corpus-builder to canonicalize PDFs/URLs into the literature wiki, enforce raw-source immutability, resolve duplicates, refresh indexes, and persist ingest decisions before handing note writing to $research-note-author."
