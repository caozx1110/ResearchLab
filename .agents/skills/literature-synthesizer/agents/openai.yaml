interface:
  display_name: "Literature Synthesizer"
  short_description: "负责 survey、taxonomy、topic/pool 级综合整理"
  default_prompt: "使用 $literature-synthesizer 基于当前 v2 units 按 query、topic、tag 或 candidate pool 生成 survey、review 与 taxonomy 视图。"
